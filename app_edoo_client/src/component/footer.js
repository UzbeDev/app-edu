import React, {Component} from 'react';
import PropTypes from 'prop-types';

class Footer extends Component {
    render() {
        return (
            <div>
                <h1 style={{textTransform:'uppercase', display:'flex'}}>Footer page</h1>
            </div>
        );
    }
}

Footer.propTypes = {};

export default Footer;
